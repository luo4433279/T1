<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org" th:with="basicUrl=@{/}">
<head>
<!--加载公共css  -->
<div th:replace="head"></div>

</head>

<body class="gray-bg">
	<div class="wrapper wrapper-content">
		<!--你的内容，多行用多个row  -->
		<div class="row">
			<div class="col-xs-12">
				<form class="form-horizontal" th:action="@{${api}}" method="post">
					<div class="box-body">

						<div class="form-group">
							<label for="inputEmail3" class="col-sm-2 control-label">名称</label>

							<div class="col-sm-10">
								<input type="text" name="title"
									th:value="${resource}? ${resource.title}" class="form-control"
									id="inputEmail3" placeholder="" required="required"
									maxlength="30" />
							</div>
						</div>


						<div class="form-group">
							<label for="inputEmail3" class="col-sm-2 control-label">url</label>

							<div class="col-sm-10">
								<input type="text" name="url"
									th:value="${resource}? ${resource.url}" class="form-control"
									id="inputEmail3" placeholder="" required="required"
									maxlength="100" />
							</div>
						</div>

				<!-- 		<div class="form-group">
							<label for="inputEmail3" class="col-sm-2 control-label">授权按钮</label>

							<div class="col-sm-10">
								<select name="buttons" class="form-control">
									<option th:each="item:${buttonsList}" th:value="${item.id}"
										th:text="${item.biko}">菜单id</option>
								</select>
								<input type="hidden" name="buttonsTemp"
                                    th:value="${resource}? ${resource.buttons}" />
							</div>
						</div> -->

						<div class="form-group">
							<label for="inputEmail3" class="col-sm-2 control-label">描述</label>

							<div class="col-sm-10">
								<input type="text" name="description"
									th:value="${resource}? ${resource.description}"
									class="form-control" id="inputEmail3" placeholder=""
									maxlength="50" />
							</div>
						</div>
						<br />
						<br />
					</div>
					<!-- /.box-body -->
					<div class="box-footer" style="text-align: center;">
						<button type="submit" class="btn btn-info">提交</button>
						<button type="button" class="btn btn-info"
							onclick="history.go(-1)">返回</button>
					</div>
					<!-- /.box-footer -->
				</form>
			</div>
		</div>
	</div>
	<!--加载公共js  -->
	<div th:replace="script"></div>
	<script>
		$(function() {//打开页面执行的方法
			
/* 			//按钮下拉项
            var buttonsTemp = $("input[name='buttonsTemp']").val();
            if(buttonsTemp!=null || buttonsTemp!=""){
                $("select[name='buttons']").find("option[value='"+buttonsTemp+"']").attr("selected",true);
            } */
            
		});
	</script>

</body>
</html>